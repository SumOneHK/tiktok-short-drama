# `AI资产圣经.md` 固定模板

> 调用场景：**唯一首次创建点**是 `/分镜脚本 {起止集}` 首次进入。其他命令**禁止**创建、禁止"降级最小版本"。

```markdown
# {剧名} AI资产圣经

## 使用原则
- 用途：作为全局角色/场景/道具/画风的稳定提示词库；`分集剧本/*.md` 只可读取叙事事实层，`分镜脚本/*.md` 继承 prompt / 风格 / 连续性锁定层
- 创建时机：首次进入 `/分镜脚本` 且项目确定进入 AI 生产阶段时
- 更新原则：只有会持续影响后续 2 集以上的稳定资产变化，才回写本文件；一次性状态变化留在单集增量附录

## 全局生成规则
- artStyle：（开放列表，例如 写实电影感 / 港风复古 / 国潮水墨 / 赛博朋克 / 厚涂插画 / 3D 卡通）
- 风格参考：
- 目标平台：
- 风格关键词模板：（可直接拼进 `AI画面提示` 的关键词串）
- 色彩基调：
- 光线语言：
- 景别偏好：
- 运镜偏好：
- 渲染质感：
- 字幕/字卡安全区：
- 全局共用负面约束：
- 目标 AI 视频模型偏好：（例如 首选可灵 / 次选 Runway / 海螺备用）

## 角色资产卡
### 角色：{角色名}
- 英文展示名：
- characterId：（稳定 ID，建议英文小写下划线，如 `lin_wan`；同一角色所有时间线/状态共用）
- 核心识别词：（一句话身份摘要，用于人类检索，不替代身份锚点）

#### 身份锚点（Identity Anchor，全剧不可变）
- 年龄基准：
- 面部几何：（脸型、眼型、鼻型、唇形、下颌线、痣/疤等固定识别点）
- 身形比例：（身高感、肩颈、体型、姿态）
- 固定气质：（不随服装变化的底层气质）
- 不可变特征：（后续所有外观状态必须继承）
- 禁止变化：（明确禁止换脸、改眼型、改脸型、改变身形比例等）

#### 外观状态（Visual States / Life Stages）
> 同一角色只能有一个角色资产卡。现在线、过去线、失忆前、复仇后等差异必须写成状态 `S01/S02/...`，不得拆成多个角色卡，也不得为每条时间线另写一套完整角色设定。

##### S01：{状态语义，如 基线-现在线医生}
- 时间线归属：（现在线 / 过去线 / 闪回-1 / 失忆前 / 失忆后 等）
- 有效范围：（如 E001-E011；若仅闪回使用，写明触发镜头/集数）
- 触发原因：（首次出场 / 长期换装 / 年代跳跃 / 身份揭露 / 持续伤势）
- 从阶段继承：（S01 写“无”；S02+ 必须写上一阶段或身份锚点）
- 必须保持不变：（从 Identity Anchor 继承的脸、身形、固定识别点）
- 允许变化：（仅列本状态允许改变的发型、服装、妆造、年龄感、伤势等）
- 年龄感：
- 发型/发色：
- 服装基线：
- 妆造/伤势：
- 气质状态：
- 常用表情/动作锚点：
- 基础 AI 画面提示：（只描述本状态的可变外观 + 必须继承 Identity Anchor；不要重写另一张脸）

##### S02：{状态语义，可按需增删}
- 时间线归属：
- 有效范围：
- 触发原因：
- 从阶段继承：
- 必须保持不变：
- 允许变化：
- 年龄感：
- 发型/发色：
- 服装基线：
- 妆造/伤势：
- 气质状态：
- 常用表情/动作锚点：
- 基础 AI 画面提示：

- 基础 AI 动作提示：
- 全局负面约束：
- 当前长期状态：
- 最近更新时间：

## 场景资产卡
### 场景：{场景名}
- 场景功能：
- 空间结构：
- 视觉识别点：
- 光线/色调：
- 常驻道具：
- 常见人物：
- 基础 AI 画面提示：
- 基础 AI 动作/运镜提示：
- 全局负面约束：
- 当前长期状态：
- 最近更新时间：

## 道具资产卡
### 道具：{道具名}
- 所属角色/场景：
- 外观与材质：
- 首登场集数：
- 功能：
- 基础 AI 画面提示：
- 连续性重点：
- 全局负面约束：
- 当前长期状态：
- 最近更新时间：

## 单集增量回写规则
- 需要回写到本文件的变化：
  - 角色长期换装 / 持续伤势 / 发型改变 / 气质口径改变
  - 场景被破坏、封锁、换光线体系、加入固定新元素
  - 道具损坏、转手、污染、解锁新状态且会持续存在
- 不直接回写本文件的变化：
  - 本集一次性的表情强调
  - 单镜头临时运镜
  - 当集专属音效点
  - 不会延续到后续集数的临时血迹/灰尘/站位

## 最近一次同步
- 已同步来源文件：
- 同步日期：
- 备注：
```
